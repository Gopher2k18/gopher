import { Cardable } from './cardable';
import { Card } from './card';

export class Events {
  title: string;
  day: string;
  month: string;
  year: string;
  organizer: string;
  venue: string;
  isStared: Boolean = false;
}
